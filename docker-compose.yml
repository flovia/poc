services:
  bff:
    build:
      context: .
      dockerfile: apps/bff/Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL:-/data/flovia.db}
      PORT: 3001
    volumes:
      - ${FLOVIA_DATA_DIR:-./docker-data}:${BFF_DATA_MOUNT_PATH:-/data}
    ports:
      - "${BFF_PORT:-3001}:3001"
    restart: unless-stopped
